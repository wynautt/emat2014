subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile 'junit:junit:4.11'
    }

    task wrapper(type: Wrapper) {
        gradleVersion = '1.10'
    }

    version = '1.0.0'
}